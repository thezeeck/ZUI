import { CreateTheme, darkTheme, hexToRgba, colorsSchema } from "../ZUI";

export const userTheme = CreateTheme({
  colors: {
    background: "#000A11"
  },
  name: "userTheme",

}, darkTheme);
import { AvatarContent } from "./content/Avatar";
import { GridContent } from "./content/Grid";

export const Main = () => {
  return (
    <main>
      <GridContent />
      <AvatarContent />
    </main>
  )
}